# Interim data

Interim data can be regenerated from raw inputs and recorded commands. Examples
include extracted archives, Cell Ranger output, matrix conversions, barcode
repairs, metadata joins, and parsed TCR tables.

Each future run must carry an input-registry snapshot, command, environment,
code commit, and output manifest.
